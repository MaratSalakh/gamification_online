export const NavBar = () => {
  return (
    <header className="flex justify-between">
      <button>Gamification</button>
      <div>
        <button>Main</button>
        <button>About</button>
      </div>
    </header>
  );
};
